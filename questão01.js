let nota1 = 79;
let nota2 = 80;
let nota3 = 70;
let nota4 = 60;
let nota

nota = (nota1 + nota2 + nota3 + nota4) / 4;

if (nota >= 60) {
    console.log("Aprovado! Com a média de: " + nota );
}
else if (nota < 40) {
    console.log("Reprovado. Com a média de: " + nota);
}
else (nota >= 40 & nota <= 50)
    console.log("Recuperação");
    
